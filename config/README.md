# Configuration

The canonical example configuration is `../configs/default.yaml`. Keep paths
portable and pass input, model, output, and shard selection through CLI
arguments or a copied local configuration. Do not commit local data paths,
model weights, production shards, or feature outputs.
